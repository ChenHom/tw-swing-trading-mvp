# Web UI 與告警額外依賴（與核心 requirements.txt 分離，便於只跑交易核心者略過）
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9   # 表單輸入（之後寫入操作用）
httpx>=0.27.0             # Discord 告警 / 一般 HTTP
